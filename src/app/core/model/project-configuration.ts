export interface ProjectConfiguration {
    topModuleFilename: string;
    testbenchFilename: string;
    outputFoldername: string;
    outputFilename: string;
    waveformFilename: string;
    simulator: string;
}
