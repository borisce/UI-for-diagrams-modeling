import { SimulationFilesCardComponent } from './simulation-files-card.component'

describe('SimulationFilesCardComponent', () => {
  it('should mount', () => {
    cy.mount(SimulationFilesCardComponent)
  })
})
